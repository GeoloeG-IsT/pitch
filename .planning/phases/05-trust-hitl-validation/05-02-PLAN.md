---
phase: 05-trust-hitl-validation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/app/globals.css
  - apps/web/components/confidence-badge.tsx
  - apps/web/components/verified-badge.tsx
  - apps/web/components/qa/verification-placeholder.tsx
  - apps/web/hooks/use-query-stream.ts
  - apps/web/hooks/use-notification-stream.ts
  - apps/web/components/qa/qa-thread.tsx
  - apps/web/lib/query-api.ts
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02

must_haves:
  truths:
    - "Investor sees colored confidence badge below answer text (green/yellow/red)"
    - "Hovering badge shows numeric percentage score"
    - "Low-confidence answers show 'being verified' placeholder instead of answer"
    - "When founder approves a queued answer, placeholder animates into approved answer with Verified badge"
    - "Founder-reviewed answers display green Verified badge with shield-check icon"
  artifacts:
    - path: "apps/web/components/confidence-badge.tsx"
      provides: "Reusable confidence tier badge with tooltip"
      contains: "ConfidenceBadge"
    - path: "apps/web/components/verified-badge.tsx"
      provides: "Verified badge for founder-reviewed answers"
      contains: "VerifiedBadge"
    - path: "apps/web/components/qa/verification-placeholder.tsx"
      provides: "Placeholder for queued answers"
      contains: "VerificationPlaceholder"
    - path: "apps/web/hooks/use-notification-stream.ts"
      provides: "WebSocket hook for receiving approved answer push"
      contains: "useNotificationStream"
    - path: "apps/web/hooks/use-query-stream.ts"
      provides: "Extended query stream hook with confidence and queued handling"
      contains: "confidence_score"
  key_links:
    - from: "apps/web/components/qa/qa-thread.tsx"
      to: "apps/web/components/confidence-badge.tsx"
      via: "import ConfidenceBadge"
      pattern: "import.*ConfidenceBadge"
    - from: "apps/web/components/qa/qa-thread.tsx"
      to: "apps/web/components/qa/verification-placeholder.tsx"
      via: "import VerificationPlaceholder"
      pattern: "import.*VerificationPlaceholder"
    - from: "apps/web/hooks/use-notification-stream.ts"
      to: "WebSocket /api/v1/notifications/stream"
      via: "WebSocket connection"
      pattern: "notifications/stream"
---

<objective>
Add investor-facing confidence badges, verification placeholders for queued answers, and real-time WebSocket push for approved answers.

Purpose: Investors see calibrated trust indicators on every AI answer and experience seamless delivery of founder-reviewed answers without refreshing.
Output: ConfidenceBadge, VerifiedBadge, VerificationPlaceholder components; extended useQueryStream hook; new useNotificationStream hook; updated QA thread.
</objective>

<execution_context>
@/home/pascal/wks/pitch/.claude/get-shit-done/workflows/execute-plan.md
@/home/pascal/wks/pitch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-hitl-validation/05-CONTEXT.md
@.planning/phases/05-trust-hitl-validation/05-RESEARCH.md
@.planning/phases/05-trust-hitl-validation/05-UI-SPEC.md
@.planning/phases/05-trust-hitl-validation/05-01-SUMMARY.md

<interfaces>
<!-- Backend API contract from Plan 01 -->

WebSocket "done" message (high/moderate confidence):
```json
{"type": "done", "query_id": "...", "confidence_score": 72.3, "confidence_tier": "high"}
```

WebSocket "queued" message (low confidence):
```json
{"type": "queued", "message": "This answer is being verified by the team -- check back shortly.", "query_id": "..."}
```

WebSocket "replace_answer" message (clean answer without CONFIDENCE: line):
```json
{"type": "replace_answer", "answer": "The cleaned answer text..."}
```

Notification WebSocket "answer_approved" message:
```json
{"type": "answer_approved", "query_id": "...", "answer": "...", "confidence_tier": "verified"}
```

From apps/web/hooks/use-query-stream.ts:
```typescript
export type QueryStatus = "idle" | "retrieving" | "generating" | "done" | "error";
export function useQueryStream(): { answer, status, citations, error, askQuestion }
```

From apps/web/components/qa/qa-thread.tsx:
```typescript
export interface QAMessage {
  id: string; question: string; answer: string; citations: Citation[];
  status: QueryStatus; error: string | null; sectionContext: string | null;
}
```

From apps/web/lib/query-api.ts:
```typescript
export interface Citation { document_id, document_title, section_number, section_label, chunk_id, relevance_score }
export interface QueryResponse { query_id, question, answer, citations, status, created_at }
```

CSS variables from globals.css (existing theme):
```css
--color-destructive: hsl(0 84.2% 60.2%);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confidence badge components and CSS variables</name>
  <files>
    apps/web/app/globals.css,
    apps/web/components/confidence-badge.tsx,
    apps/web/components/verified-badge.tsx,
    apps/web/components/qa/verification-placeholder.tsx
  </files>
  <read_first>
    apps/web/app/globals.css,
    apps/web/components/ui/badge.tsx,
    apps/web/components/ui/tooltip.tsx,
    apps/web/lib/utils.ts,
    .planning/phases/05-trust-hitl-validation/05-UI-SPEC.md
  </read_first>
  <action>
    1. Add CSS variables to `apps/web/app/globals.css` inside the `@theme inline` block:
       ```css
       --color-confidence-high: hsl(142 71% 45%);
       --color-confidence-moderate: hsl(38 92% 50%);
       --color-confidence-low: hsl(0 84% 60%);
       --color-verified: hsl(142 71% 45%);
       ```
       Also add dark mode variants inside `.dark` block:
       ```css
       --color-confidence-high: hsl(142 71% 35%);
       --color-confidence-moderate: hsl(38 92% 40%);
       --color-confidence-low: hsl(0 63% 31%);
       --color-verified: hsl(142 71% 35%);
       ```

    2. Create `apps/web/components/confidence-badge.tsx`:
       - Import `Badge` from `@/components/ui/badge`, `Tooltip`/`TooltipContent`/`TooltipProvider`/`TooltipTrigger` from `@/components/ui/tooltip`.
       - Define `tierConfig` object mapping tier strings to label and className:
         - "high": label "High confidence", className "bg-[hsl(142,71%,45%)] text-white hover:bg-[hsl(142,71%,40%)]"
         - "moderate": label "Moderate confidence", className "bg-[hsl(38,92%,50%)] text-white hover:bg-[hsl(38,92%,45%)]"
         - "low": label "Low confidence", className "bg-destructive text-destructive-foreground hover:bg-destructive/90"
       - Props: `{ tier: "high" | "moderate" | "low"; score: number }`.
       - Render: TooltipProvider > Tooltip > TooltipTrigger(asChild) > Badge with tier className and label text. TooltipContent shows "Score: {score}%".
       - Export as named export `ConfidenceBadge`.

    3. Create `apps/web/components/verified-badge.tsx`:
       - Import `Badge` from `@/components/ui/badge`, `ShieldCheck` from `lucide-react`.
       - Render: Badge with className "bg-[hsl(142,71%,45%)] text-white hover:bg-[hsl(142,71%,40%)] gap-1" containing `<ShieldCheck className="h-3.5 w-3.5" />` and text "Verified".
       - Export as named export `VerifiedBadge`.

    4. Create `apps/web/components/qa/verification-placeholder.tsx`:
       - Simple component rendering a muted card-like area with the message: "This answer is being verified by the team -- check back shortly."
       - Uses `text-sm text-muted-foreground` styling with a subtle `bg-muted rounded-lg p-4` container.
       - Include a small pulsing dot indicator (div with `animate-pulse bg-amber-400 rounded-full h-2 w-2 inline-block mr-2`) before the message text.
       - Export as named export `VerificationPlaceholder`.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/globals.css contains "--color-confidence-high: hsl(142 71% 45%)"
    - apps/web/app/globals.css contains "--color-confidence-moderate: hsl(38 92% 50%)"
    - apps/web/app/globals.css contains "--color-verified: hsl(142 71% 45%)"
    - apps/web/components/confidence-badge.tsx contains "export function ConfidenceBadge"
    - apps/web/components/confidence-badge.tsx contains "High confidence"
    - apps/web/components/confidence-badge.tsx contains "Moderate confidence"
    - apps/web/components/confidence-badge.tsx contains "Low confidence"
    - apps/web/components/confidence-badge.tsx contains "Score:"
    - apps/web/components/verified-badge.tsx contains "export function VerifiedBadge"
    - apps/web/components/verified-badge.tsx contains "ShieldCheck"
    - apps/web/components/verified-badge.tsx contains "Verified"
    - apps/web/components/qa/verification-placeholder.tsx contains "export function VerificationPlaceholder"
    - apps/web/components/qa/verification-placeholder.tsx contains "being verified by the team"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Three new components (ConfidenceBadge, VerifiedBadge, VerificationPlaceholder) created with correct styling per UI-SPEC. CSS variables added for confidence tier colors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend hooks for confidence/queued handling and integrate into QA thread</name>
  <files>
    apps/web/hooks/use-query-stream.ts,
    apps/web/hooks/use-notification-stream.ts,
    apps/web/components/qa/qa-thread.tsx,
    apps/web/components/qa/qa-panel.tsx,
    apps/web/lib/query-api.ts
  </files>
  <read_first>
    apps/web/hooks/use-query-stream.ts,
    apps/web/components/qa/qa-thread.tsx,
    apps/web/components/qa/qa-panel.tsx,
    apps/web/lib/query-api.ts,
    apps/web/components/confidence-badge.tsx,
    apps/web/components/verified-badge.tsx,
    apps/web/components/qa/verification-placeholder.tsx
  </read_first>
  <action>
    1. Extend `apps/web/lib/query-api.ts`:
       - Add to `QueryResponse` interface: `confidence_score: number | null`, `confidence_tier: string | null`, `review_status: string`.

    2. Extend `apps/web/hooks/use-query-stream.ts`:
       - Add new state: `const [confidenceScore, setConfidenceScore] = useState<number | null>(null)` and `const [confidenceTier, setConfidenceTier] = useState<string | null>(null)`.
       - Add new state: `const [isQueued, setIsQueued] = useState(false)`.
       - Add `queryId` state: `const [queryId, setQueryId] = useState<string | null>(null)`.
       - Update `QueryStatus` type to include "queued": `export type QueryStatus = "idle" | "retrieving" | "generating" | "done" | "queued" | "error"`.
       - In reset block at start of `askQuestion`: reset `confidenceScore`, `confidenceTier`, `isQueued`, `queryId` to null/false/null.
       - After `createQuery`, set `queryId` from response.
       - Handle new WS message types in `onmessage`:
         - `"queued"`: `setIsQueued(true)`, `setStatus("queued")`, `setAnswer(msg.message)` (the placeholder text).
         - `"done"` (existing): also `setConfidenceScore(msg.confidence_score)`, `setConfidenceTier(msg.confidence_tier)`.
         - `"replace_answer"`: `setAnswer(msg.answer)` (replaces the streamed answer with clean version, stripping the CONFIDENCE: line).
       - Return: add `confidenceScore`, `confidenceTier`, `isQueued`, `queryId` to returned object.

    3. Create `apps/web/hooks/use-notification-stream.ts`:
       - New hook `useNotificationStream(onAnswerApproved: (queryId: string, answer: string, tier: string) => void)`.
       - Opens a persistent WebSocket to `${NEXT_PUBLIC_WS_URL}/api/v1/notifications/stream` (using same env var pattern as use-query-stream).
       - On `message` event: parse JSON, if `type === "answer_approved"`, call `onAnswerApproved(msg.query_id, msg.answer, msg.confidence_tier)`.
       - Auto-reconnect with 3-second delay on close (simple reconnect, not exponential backoff -- PoC scope).
       - Cleanup on unmount: close WebSocket, clear reconnect timeout.
       - Return `{ connected: boolean }`.

    4. Update `apps/web/components/qa/qa-thread.tsx`:
       - Add to `QAMessage` interface: `confidenceScore: number | null`, `confidenceTier: string | null`, `isQueued: boolean`, `isVerified: boolean`.
       - Import `ConfidenceBadge` from `@/components/confidence-badge`, `VerifiedBadge` from `@/components/verified-badge`, `VerificationPlaceholder` from `@/components/qa/verification-placeholder`.
       - In the message rendering, after the answer area and before citations:
         - If `msg.isQueued && !msg.isVerified`: render `<VerificationPlaceholder />` instead of the answer + citations.
         - If `msg.isVerified`: render the answer normally + `<VerifiedBadge />` below the answer (before citations).
         - If `msg.confidenceTier && !msg.isQueued && !msg.isVerified`: render `<ConfidenceBadge tier={msg.confidenceTier} score={msg.confidenceScore || 0} />` below the answer, before citations.
       - Add transition animation: when `isQueued` changes to `isVerified`, use CSS `transition-opacity duration-300` for fade effect.

    5. Update `apps/web/components/qa/qa-panel.tsx`:
       - Import `useNotificationStream` from `@/hooks/use-notification-stream`.
       - In the message snapshot effect, add `confidenceScore`, `confidenceTier`, `isQueued`, `isVerified: false` to each QAMessage.
       - Call `useNotificationStream` with a callback that finds the matching message by query_id and updates it: set `isQueued=false`, `isVerified=true`, `answer=approved_answer`, `status="done"`.
       - This enables the real-time push: when founder approves, the placeholder in the investor's thread smoothly becomes the approved answer with a Verified badge.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/hooks/use-query-stream.ts contains "confidenceScore"
    - apps/web/hooks/use-query-stream.ts contains "confidenceTier"
    - apps/web/hooks/use-query-stream.ts contains "isQueued"
    - apps/web/hooks/use-query-stream.ts contains '"queued"' in the QueryStatus type or switch case
    - apps/web/hooks/use-query-stream.ts contains "replace_answer"
    - apps/web/hooks/use-notification-stream.ts contains "export function useNotificationStream"
    - apps/web/hooks/use-notification-stream.ts contains "answer_approved"
    - apps/web/hooks/use-notification-stream.ts contains "notifications/stream"
    - apps/web/components/qa/qa-thread.tsx contains "ConfidenceBadge"
    - apps/web/components/qa/qa-thread.tsx contains "VerifiedBadge"
    - apps/web/components/qa/qa-thread.tsx contains "VerificationPlaceholder"
    - apps/web/components/qa/qa-thread.tsx contains "isQueued"
    - apps/web/components/qa/qa-thread.tsx contains "isVerified"
    - apps/web/lib/query-api.ts contains "confidence_score"
    - apps/web/lib/query-api.ts contains "review_status"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Investor-facing confidence badges display on all AI answers (green/yellow/red), queued answers show verification placeholder, founder-approved answers push via WebSocket and replace placeholder with Verified badge.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes with no type errors
- ConfidenceBadge renders three tiers with correct colors and tooltip
- VerifiedBadge renders green badge with ShieldCheck icon
- VerificationPlaceholder shows the "being verified" message
- useQueryStream handles "done" (with confidence), "queued", and "replace_answer" message types
- useNotificationStream connects to notification WebSocket and handles "answer_approved"
- QAThread renders appropriate badge/placeholder based on message state
</verification>

<success_criteria>
- Every AI response in the QA thread displays a confidence badge (TRUST-01 investor-facing)
- Low-confidence answers show "being verified" placeholder instead of the AI answer (TRUST-02 investor-facing)
- When founder approves via dashboard, placeholder smoothly transitions to approved answer with Verified badge (TRUST-02 real-time push)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-trust-hitl-validation/05-02-SUMMARY.md`
</output>

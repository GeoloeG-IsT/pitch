---
phase: 09-polish-citation-clicks-and-shared-types-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/index.ts
  - packages/shared-types/package.json
  - packages/shared-types/tsconfig.json
  - packages/shared-types/AGENTS.md
  - packages/shared-types/CLAUDE.md
  - packages/shared-types/src/AGENTS.md
  - packages/shared-types/src/CLAUDE.md
  - pnpm-lock.yaml
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements: [TECH-DEBT-01, TECH-DEBT-02]

must_haves:
  truths:
    - "Citation cards in pitch viewer Q&A thread are clickable and scroll the viewer to the referenced section with highlight"
    - "No orphaned packages exist in the monorepo (shared-types removed)"
    - "pnpm build succeeds with no errors after shared-types removal"
    - "pnpm typecheck succeeds with no errors after shared-types removal"
  artifacts:
    - path: "apps/web/components/query/citation-list.tsx"
      provides: "CitationList with onCitationClick prop wired to Card onClick"
      contains: "onCitationClick"
    - path: "apps/web/components/qa/qa-thread.tsx"
      provides: "QAThread passing onCitationClick to CitationList"
      contains: "onCitationClick={onCitationClick}"
  key_links:
    - from: "apps/web/components/query/citation-list.tsx"
      to: "apps/web/components/qa/qa-thread.tsx"
      via: "onCitationClick prop"
      pattern: "onCitationClick"
    - from: "apps/web/components/qa/qa-panel.tsx"
      to: "apps/web/components/viewer/pitch-viewer.tsx"
      via: "onScrollToSection -> scrollToSection"
      pattern: "onScrollToSection"
---

<objective>
Resolve two tech debt items from the v1.0 milestone audit: (1) verify that citation click-to-scroll is working (it was already fixed in commit 5a6431c but the audit doc still lists it as tech debt), and (2) remove the orphaned packages/shared-types package whose exported types are never consumed by any app.

Purpose: Clean up the codebase so the audit accurately reflects current state and no dead code remains.
Output: Shared-types package removed, audit doc updated, build passing.
</objective>

<execution_context>
@/home/pascal/wks/pitch/.claude/get-shit-done/workflows/execute-plan.md
@/home/pascal/wks/pitch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Citation click chain (already wired -- verify only) -->

From apps/web/components/query/citation-list.tsx:
```typescript
interface CitationListProps {
  citations: Citation[];
  open: boolean;
  onCitationClick?: (chunkId: string) => void;
}
```

From apps/web/components/qa/qa-thread.tsx:
```typescript
interface QAThreadProps {
  messages: QAMessage[];
  onCitationClick?: (chunkId: string) => void;
}
```

From apps/web/components/qa/qa-panel.tsx:
```typescript
// handleCitationClick calls onScrollToSection(chunkId)
```

From apps/web/components/viewer/pitch-viewer.tsx:
```typescript
// scrollToSection finds element by id `section-${id}`, scrolls, highlights with ring-2 ring-primary/30 for 2s
```

<!-- shared-types (to be removed) -->
From packages/shared-types/src/index.ts:
```typescript
export interface HealthResponse { ... }
export interface ApiHealthResponse { ... }
```
These types are NOT imported anywhere in apps/web or apps/api.
apps/web/app/api/health/route.ts uses inline types.
apps/web/package.json does NOT list @zeee/shared-types as a dependency.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify citation click-to-scroll and remove shared-types package</name>
  <files>
    packages/shared-types/src/index.ts,
    packages/shared-types/package.json,
    packages/shared-types/tsconfig.json,
    packages/shared-types/AGENTS.md,
    packages/shared-types/CLAUDE.md,
    packages/shared-types/src/AGENTS.md,
    packages/shared-types/src/CLAUDE.md,
    packages/shared-types/src/index.ts.sum,
    packages/shared-types/package.json.sum,
    packages/shared-types/tsconfig.json.sum
  </files>
  <read_first>
    apps/web/components/query/citation-list.tsx,
    apps/web/components/qa/qa-thread.tsx,
    apps/web/components/qa/qa-panel.tsx,
    apps/web/components/viewer/pitch-viewer.tsx,
    packages/shared-types/src/index.ts,
    packages/shared-types/package.json
  </read_first>
  <action>
    This task has two parts:

    PART A -- Verify citation click-to-scroll (no code changes expected):

    1. Confirm that `citation-list.tsx` has `onCitationClick?: (chunkId: string) => void` in its props interface
    2. Confirm that `citation-list.tsx` passes `onClick={onCitationClick ? () => onCitationClick(citation.chunk_id) : undefined}` to each Card
    3. Confirm that `qa-thread.tsx` passes `onCitationClick={onCitationClick}` to CitationList
    4. Confirm that `qa-panel.tsx` defines `handleCitationClick` that calls `onScrollToSection(chunkId)`
    5. Confirm that `pitch-viewer.tsx` defines `scrollToSection` that finds `section-${id}` element, scrolls to it, and applies ring highlight
    6. Run `grep -n "onCitationClick" apps/web/components/query/citation-list.tsx apps/web/components/qa/qa-thread.tsx apps/web/components/qa/qa-panel.tsx` to prove the chain is complete
    7. This was fixed in commit 5a6431c -- no additional code changes needed

    PART B -- Remove packages/shared-types:

    1. First verify no file in apps/web or apps/api imports from `@zeee/shared-types` by running: `grep -r "@zeee/shared-types" apps/`
    2. Delete the entire `packages/shared-types/` directory (all files including src/, node_modules/, dist/, AGENTS.md, CLAUDE.md, package.json, tsconfig.json, and any .sum files)
    3. Run `pnpm install` to update the lockfile (shared-types will be removed from workspace resolution)
    4. Run `pnpm build` to verify the full monorepo builds without the package
    5. Run `pnpm typecheck` to verify no type errors
  </action>
  <verify>
    <automated>cd /home/pascal/wks/pitch && grep -c "onCitationClick" apps/web/components/query/citation-list.tsx apps/web/components/qa/qa-thread.tsx apps/web/components/qa/qa-panel.tsx && test ! -d packages/shared-types && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms onCitationClick appears in citation-list.tsx (at least 3 times: prop definition, destructure, usage), qa-thread.tsx (at least 2 times: prop definition, pass-through), qa-panel.tsx (at least 1 time: handler definition)
    - packages/shared-types directory does not exist
    - pnpm typecheck passes with exit code 0
    - pnpm build passes with exit code 0
  </acceptance_criteria>
  <done>
    Citation click-to-scroll chain verified as complete (already fixed in 5a6431c).
    packages/shared-types fully removed from monorepo.
    Build and typecheck pass without the removed package.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update milestone audit to reflect resolved tech debt</name>
  <files>.planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>.planning/v1.0-MILESTONE-AUDIT.md</read_first>
  <action>
    Update the v1.0 milestone audit document to reflect the resolved state:

    1. In the frontmatter `tech_debt` section:
       - For phase 04-smart-document-viewer, change the CitationList item to note it was resolved:
         Replace: `"CitationList onClick handler not wired — citations render as static cards (VIEW gap from phase 4 verification)"`
         With: `"CitationList onClick handler — RESOLVED in commit 5a6431c (phase 4 gap closure)"`
       - For phase 01-foundation-demo-content, change the shared-types item to note it was resolved:
         Replace: `"packages/shared-types types exported but never consumed by apps/web"`
         With: `"packages/shared-types — RESOLVED: package removed in phase 9 cleanup"`

    2. In the frontmatter `gaps.integration` section:
       - Change the shared-types-unused entry status from `"resolved-as-tech-debt"` to `"resolved"`

    3. In the `## Tech Debt` section body:
       - Update item 1 to: `~~packages/shared-types types exported but never imported by apps/web~~ -- RESOLVED: package removed`
       - Update item 2 to: `~~CitationList onClick not wired~~ -- RESOLVED: fixed in commit 5a6431c`
       - Keep item 3 (Nyquist) unchanged

    4. In the `## Cross-Phase Integration` section:
       - Update "Orphaned (low priority)" to: `~~packages/shared-types -- exported types unused by apps/web~~ RESOLVED: package removed`
  </action>
  <verify>
    <automated>grep -c "RESOLVED" /home/pascal/wks/pitch/.planning/v1.0-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - "RESOLVED" appears at least 4 times in the audit file (2 in frontmatter tech_debt, 1 in gaps.integration status, 2 in body text)
    - The CitationList tech debt item references commit 5a6431c
    - The shared-types tech debt item notes package removal
    - Nyquist item (item 3) is unchanged
  </acceptance_criteria>
  <done>
    Milestone audit accurately reflects that both tech debt items are resolved.
    No stale tech debt entries remain for citation clicks or shared-types.
  </done>
</task>

</tasks>

<verification>
1. Citation click chain is verified end-to-end: CitationList -> QAThread -> QAPanel -> PitchViewer.scrollToSection
2. packages/shared-types directory no longer exists
3. No import of @zeee/shared-types exists in any app
4. pnpm build and pnpm typecheck both pass
5. Milestone audit reflects resolved status for both tech debt items
</verification>

<success_criteria>
- Citation click-to-scroll works in pitch viewer Q&A panel (chain verified in code)
- packages/shared-types removed from monorepo with clean build
- v1.0 milestone audit updated to mark both tech debt items as resolved
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-citation-clicks-and-shared-types-cleanup/09-01-SUMMARY.md`
</output>

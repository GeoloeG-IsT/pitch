---
phase: 01-foundation-demo-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .env.example
  - .gitignore
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/api/health/route.ts
  - apps/web/app/globals.css
  - apps/web/components.json
  - apps/web/lib/utils.ts
  - apps/api/package.json
  - apps/api/pyproject.toml
  - apps/api/app/__init__.py
  - apps/api/app/main.py
  - apps/api/app/core/config.py
  - apps/api/app/api/__init__.py
  - apps/api/app/api/v1/__init__.py
  - apps/api/app/api/v1/health.py
  - apps/api/tests/test_health.py
  - packages/shared-types/package.json
  - packages/shared-types/tsconfig.json
  - packages/shared-types/src/index.ts
  - supabase/config.toml
  - supabase/migrations/00001_init.sql
autonomous: false
requirements: []

must_haves:
  truths:
    - "Running `turbo dev` starts both Next.js (port 3000) and FastAPI (port 8000) concurrently"
    - "GET /api/v1/health on FastAPI returns JSON with status ok"
    - "Next.js health page renders three service status cards (Frontend, Backend API, Supabase)"
    - "Supabase migration creates users, documents, and chunks tables with pgvector enabled"
    - "Frontend proxies /api/v1/* requests to FastAPI backend via Next.js rewrites"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Health status page with three service cards"
      min_lines: 40
    - path: "apps/api/app/main.py"
      provides: "FastAPI application entry point"
      exports: ["app"]
    - path: "apps/api/app/api/v1/health.py"
      provides: "Health check endpoint"
      contains: "async def health_check"
    - path: "supabase/migrations/00001_init.sql"
      provides: "Core schema with pgvector"
      contains: "CREATE EXTENSION"
    - path: "turbo.json"
      provides: "Turborepo task configuration"
      contains: "tasks"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "http://localhost:8000/api/v1/*"
      via: "rewrites configuration"
      pattern: "destination.*localhost:8000"
    - from: "apps/web/app/api/health/route.ts"
      to: "/api/v1/health"
      via: "fetch call to backend"
      pattern: "fetch.*api/v1/health"
    - from: "apps/api/app/main.py"
      to: "apps/api/app/api/v1/health.py"
      via: "router inclusion"
      pattern: "include_router.*health_router"
---

<objective>
Scaffold the Turborepo monorepo with running Next.js frontend, FastAPI backend, Supabase schema, and a health status page that shows all three services are operational.

Purpose: Establishes the foundational project structure, build system, and database schema that every subsequent phase builds on. The health page provides immediate visual confirmation that the full stack is wired correctly.
Output: Complete monorepo with `turbo dev` starting both services, Supabase migration ready, health page rendering service status.
</objective>

<execution_context>
@/home/pascal/wks/pitch/.claude/get-shit-done/workflows/execute-plan.md
@/home/pascal/wks/pitch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-demo-content/01-CONTEXT.md
@.planning/phases/01-foundation-demo-content/01-RESEARCH.md
@.planning/phases/01-foundation-demo-content/01-UI-SPEC.md
@.planning/phases/01-foundation-demo-content/01-VALIDATION.md

<interfaces>
<!-- Greenfield project -- no existing interfaces. All contracts defined in this plan. -->

API contract (FastAPI health endpoint):
```
GET /api/v1/health -> { "status": "ok", "service": "zeee-pitch-zooo-api", "version": "0.1.0" }
```

Next.js health route (proxied check):
```
GET /api/health -> { "status": "healthy"|"degraded", "frontend": "ok", "backend": "ok"|"degraded"|"unreachable", "database": "ok"|"unreachable", "timestamp": ISO8601 }
```

Supabase schema tables: users, documents, chunks (with pgvector embedding column)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Turborepo monorepo with Next.js, FastAPI, shared-types, and Supabase schema</name>
  <files>
    package.json, pnpm-workspace.yaml, turbo.json, .env.example, .gitignore,
    apps/web/package.json, apps/web/tsconfig.json, apps/web/next.config.ts,
    apps/web/app/layout.tsx, apps/web/app/globals.css,
    apps/api/package.json, apps/api/pyproject.toml,
    apps/api/app/__init__.py, apps/api/app/main.py,
    apps/api/app/core/__init__.py, apps/api/app/core/config.py,
    apps/api/app/api/__init__.py, apps/api/app/api/v1/__init__.py, apps/api/app/api/v1/health.py,
    apps/api/app/models/__init__.py,
    apps/api/tests/__init__.py, apps/api/tests/test_health.py,
    packages/shared-types/package.json, packages/shared-types/tsconfig.json, packages/shared-types/src/index.ts,
    supabase/config.toml, supabase/migrations/00001_init.sql
  </files>
  <read_first>
    .planning/phases/01-foundation-demo-content/01-RESEARCH.md
    .planning/phases/01-foundation-demo-content/01-CONTEXT.md
  </read_first>
  <action>
Create the full monorepo structure from scratch. This is a greenfield project with no existing code.

**Root configuration:**

1. `package.json` -- Root package.json:
```json
{
  "name": "zeee-pitch-zooo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:reset": "supabase db reset",
    "db:migrate": "supabase migration new",
    "content:build": "cd content && uv run python build-all.py"
  },
  "devDependencies": {
    "turbo": "^2.8.17",
    "supabase": "^2.81.0"
  },
  "packageManager": "pnpm@10.32.1"
}
```

2. `pnpm-workspace.yaml`:
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

3. `turbo.json`:
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
```

4. `.env.example`:
```
# Supabase
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Backend
BACKEND_URL=http://localhost:8000
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_KEY=your-service-role-key
```

5. `.gitignore` -- Include standard Node, Python, Next.js, Supabase ignores: `node_modules/`, `.next/`, `__pycache__/`, `.venv/`, `.env`, `.env.local`, `dist/`, `.turbo/`, `content/output/`

**Frontend (apps/web):**

6. `apps/web/package.json` -- name: `@zeee/web`, dependencies: `next@^16.1.7`, `react`, `react-dom`, `@supabase/supabase-js@^2.99.2`, `@supabase/ssr@^0.9.0`. devDependencies: `typescript@^5.9.3`, `@types/react`, `@types/node`. Scripts: `dev: next dev`, `build: next build`, `start: next start`, `lint: next lint`, `typecheck: tsc --noEmit`

7. `apps/web/next.config.ts` -- Configure rewrites to proxy `/api/v1/:path*` to `http://localhost:8000/api/v1/:path*` using `process.env.BACKEND_URL || 'http://localhost:8000'` as base URL

8. `apps/web/tsconfig.json` -- Standard Next.js tsconfig with `@/*` path alias pointing to `./*`

9. `apps/web/app/layout.tsx` -- Root layout with Inter font from `next/font/google`, html lang="en", body with font class applied. Metadata: title "Zeee Pitch Zooo", description "AI-powered interactive pitch platform"

10. `apps/web/app/globals.css` -- Import Tailwind CSS. Include shadcn CSS variables for the default/New York theme in both `:root` (light) and `.dark` selectors. Use the exact shadcn default HSL values.

**Backend (apps/api):**

11. `apps/api/package.json` -- Turborepo wrapper:
```json
{
  "name": "@zeee/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "uv sync",
    "lint": "uv run ruff check .",
    "test": "uv run pytest tests/ -x",
    "typecheck": "uv run mypy app/"
  }
}
```

12. `apps/api/pyproject.toml` -- name: `zeee-api`, version: `0.1.0`, requires-python: `>=3.11`. Dependencies: `fastapi>=0.135.0`, `uvicorn`, `python-dotenv`, `supabase`. Dev dependencies: `pytest`, `httpx` (for FastAPI TestClient), `ruff`, `mypy`

13. `apps/api/app/main.py` -- FastAPI app with CORS middleware allowing `http://localhost:3000`. Include health router at prefix `/api/v1`. Title: "Zeee Pitch Zooo API", version: "0.1.0"

14. `apps/api/app/core/config.py` -- Pydantic BaseSettings class `Settings` with fields: `supabase_url: str = "http://127.0.0.1:54321"`, `supabase_key: str = ""`, `environment: str = "development"`. Load from `.env` file using `model_config = SettingsConfigDict(env_file=".env")`

15. `apps/api/app/api/v1/health.py` -- APIRouter with GET `/health` endpoint. Returns `{"status": "ok", "service": "zeee-pitch-zooo-api", "version": "0.1.0"}`. Add a Supabase connectivity check: try to connect using settings, return `"database": "ok"` or `"database": "unreachable"` with try/except

16. `apps/api/tests/test_health.py` -- Use `httpx.AsyncClient` with FastAPI's `ASGITransport` for async testing. Test: `test_health_returns_ok` -- GET `/api/v1/health`, assert status 200, assert `response.json()["status"] == "ok"`, assert `"service"` key present

**Shared types (packages/shared-types):**

17. `packages/shared-types/package.json` -- name: `@zeee/shared-types`, main: `dist/index.js`, types: `dist/index.d.ts`. Scripts: `build: tsc`, `typecheck: tsc --noEmit`

18. `packages/shared-types/tsconfig.json` -- Extend root tsconfig patterns. Target: ESNext, module: ESNext, declaration: true, outDir: dist, rootDir: src

19. `packages/shared-types/src/index.ts` -- Export API contract types:
```typescript
export interface HealthResponse {
  status: 'healthy' | 'degraded';
  frontend: 'ok';
  backend: 'ok' | 'degraded' | 'unreachable';
  database: 'ok' | 'unreachable';
  timestamp: string;
}

export interface ApiHealthResponse {
  status: 'ok';
  service: string;
  version: string;
  database?: 'ok' | 'unreachable';
}
```

**Supabase:**

20. Initialize Supabase project: run `supabase init` in project root (creates `supabase/` directory with `config.toml`)

21. `supabase/migrations/00001_init.sql` -- Full schema per CONTEXT.md decisions:
- `CREATE EXTENSION IF NOT EXISTS vector WITH SCHEMA public;`
- `CREATE TABLE public.users` -- id UUID PK references auth.users(id) ON DELETE CASCADE, email TEXT NOT NULL, display_name TEXT, role TEXT NOT NULL DEFAULT 'founder' CHECK (role IN ('founder', 'investor', 'admin')), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
- `CREATE TABLE public.documents` -- id UUID PK DEFAULT gen_random_uuid(), user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE, title TEXT NOT NULL, file_name TEXT NOT NULL, file_type TEXT NOT NULL CHECK (file_type IN ('pdf', 'xlsx', 'md', 'txt')), file_size_bytes BIGINT, status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'processing', 'ready', 'error')), metadata JSONB DEFAULT '{}', view_count INT DEFAULT 0, last_viewed_at TIMESTAMPTZ, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
- `CREATE TABLE public.chunks` -- id UUID PK DEFAULT gen_random_uuid(), document_id UUID NOT NULL REFERENCES public.documents(id) ON DELETE CASCADE, content TEXT NOT NULL, embedding vector(1536), section_number INT, page_number INT, chunk_type TEXT NOT NULL DEFAULT 'text' CHECK (chunk_type IN ('text', 'table', 'heading', 'image_caption')), metadata JSONB DEFAULT '{}', token_count INT, created_at TIMESTAMPTZ NOT NULL DEFAULT now()
- HNSW index (not IVFFlat per research recommendation): `CREATE INDEX ON public.chunks USING hnsw (embedding vector_cosine_ops)`
- Enable RLS on all three tables
- RLS policies: users can view own profile, users can view own documents, users can insert own documents, users can view chunks of own documents (subquery on documents.user_id = auth.uid())

After creating all files, run:
```bash
pnpm install
cd apps/api && uv sync
```

Run `supabase init` if supabase/ directory doesn't exist, then create the migration file.
  </action>
  <verify>
    <automated>cd /home/pascal/wks/pitch && pnpm install && cd apps/api && uv sync && cd /home/pascal/wks/pitch && cd apps/api && uv run pytest tests/test_health.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"name": "zeee-pitch-zooo"` and `"turbo": "^2.8.17"`
    - `pnpm-workspace.yaml` contains `- 'apps/*'` and `- 'packages/*'`
    - `turbo.json` contains `"tasks"` with `"build"`, `"dev"`, `"test"` entries
    - `apps/web/next.config.ts` contains `destination` and `localhost:8000`
    - `apps/api/package.json` contains `"name": "@zeee/api"` and `"dev": "uv run uvicorn`
    - `apps/api/pyproject.toml` contains `fastapi` and `pytest`
    - `apps/api/app/main.py` contains `include_router` and `CORSMiddleware`
    - `apps/api/app/api/v1/health.py` contains `async def health_check` and `"status": "ok"`
    - `apps/api/tests/test_health.py` contains `test_health_returns_ok` and exits 0
    - `supabase/migrations/00001_init.sql` contains `CREATE EXTENSION IF NOT EXISTS vector`, `CREATE TABLE public.users`, `CREATE TABLE public.documents`, `CREATE TABLE public.chunks`, `vector(1536)`, `USING hnsw`, `ENABLE ROW LEVEL SECURITY`
    - `packages/shared-types/src/index.ts` contains `HealthResponse` and `ApiHealthResponse`
    - `.env.example` contains `NEXT_PUBLIC_SUPABASE_URL` and `BACKEND_URL`
  </acceptance_criteria>
  <done>
    Monorepo scaffolded with all directories. `pnpm install` succeeds. `uv sync` succeeds in apps/api. Pytest health test passes. Supabase migration file exists with correct schema. Shared types package exports API contracts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build health status page with shadcn components and Next.js health API route</name>
  <files>
    apps/web/app/page.tsx,
    apps/web/app/api/health/route.ts,
    apps/web/components.json,
    apps/web/lib/utils.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-demo-content/01-UI-SPEC.md
    apps/web/app/layout.tsx
    apps/web/app/globals.css
    apps/web/package.json
  </read_first>
  <action>
Install shadcn and build the health status page per the UI-SPEC.md design contract.

**Step 1: Initialize shadcn/ui**
```bash
cd apps/web && npx shadcn@latest init
```
Accept defaults: New York style, CSS variables enabled, Tailwind v4, `@/components/ui` path. This creates `components.json` and `lib/utils.ts`.

**Step 2: Add shadcn components**
```bash
cd apps/web && npx shadcn@latest add badge card
```

**Step 3: Create `/api/health` route handler** (`apps/web/app/api/health/route.ts`):
- GET handler that fetches `${process.env.BACKEND_URL || 'http://localhost:8000'}/api/v1/health` with 3-second timeout (`AbortSignal.timeout(3000)`)
- On success: parse JSON, extract `status` and `database` fields from backend response
- Return JSON: `{ status: "healthy"|"degraded", frontend: "ok", backend: "ok"|"degraded"|"unreachable", database: backend_data.database || "unreachable", timestamp: new Date().toISOString() }`
- On fetch error (backend unreachable): return `{ status: "degraded", frontend: "ok", backend: "unreachable", database: "unreachable", timestamp }` with HTTP 503
- Use `NextResponse.json()` for responses

**Step 4: Create health page** (`apps/web/app/page.tsx`):
This is a `"use client"` component.

Layout (per UI-SPEC):
- Outer: `min-h-screen flex items-center justify-center` with `bg-background`
- Inner container: `w-full max-w-[480px] px-4`
- Top padding: 64px (3xl), bottom padding: 64px (3xl)
- Title: "Zeee Pitch Zooo" -- 28px semibold (text-[28px] font-semibold leading-[1.2]), centered, text-primary
- Subtitle: "System Status" -- 14px regular (text-sm), centered, text-muted-foreground
- Gap between header and cards: 32px (xl)
- Three service cards using shadcn `Card` component:
  - Each card: flex row, justify-between, items-center, p-4
  - Card gap between cards: 8px (sm) -- use `space-y-2` or `gap-2`
  - Left side: service name (16px regular)
  - Right side: shadcn `Badge` with status-specific styling

Service card labels and badge styling:
- "Frontend" -- always shows green badge "Healthy" (`bg-green-500 text-white hover:bg-green-500`)
- "Backend API" -- badge based on `backend` field: "Healthy" (green), "Degraded" (yellow: `bg-yellow-500 text-black hover:bg-yellow-500`), "Unreachable" (red: `bg-destructive text-destructive-foreground hover:bg-destructive`)
- "Supabase" -- badge based on `database` field: same color mapping as Backend

Summary message below cards (24px gap = lg):
- All healthy: "All systems operational" -- text-muted-foreground, centered
- Any degraded/unreachable: "Some services are not responding. Check that all services are running." -- text-muted-foreground, centered
- Multiple down: "Multiple services are down. Run `turbo dev` and `supabase start` to start all services." -- text-muted-foreground, centered

Footer at bottom (64px gap = 3xl from summary):
- "Phase 1 -- Foundation" -- text-sm text-muted-foreground, centered

Data fetching:
- Use `useState` for health data and loading state
- Use `useEffect` to fetch `/api/health` on mount
- Loading state: render three cards with skeleton pulse animation (use a simple `animate-pulse bg-muted rounded h-[52px]` placeholder for each card)
- No polling, no auto-refresh -- manual browser reload only
  </action>
  <verify>
    <automated>cd /home/pascal/wks/pitch/apps/web && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/page.tsx` contains `"use client"`, `"Zeee Pitch Zooo"`, `"System Status"`, `"Frontend"`, `"Backend API"`, `"Supabase"`, `"All systems operational"`, `"Phase 1 -- Foundation"`
    - `apps/web/app/page.tsx` contains `useEffect` and `useState` and `fetch('/api/health')`
    - `apps/web/app/page.tsx` contains `bg-green-500` and `bg-yellow-500` and `bg-destructive`
    - `apps/web/app/page.tsx` contains `max-w-[480px]` and `min-h-screen`
    - `apps/web/app/page.tsx` contains `animate-pulse` (loading state)
    - `apps/web/app/api/health/route.ts` contains `export async function GET` and `AbortSignal.timeout(3000)` and `NextResponse.json`
    - `apps/web/app/api/health/route.ts` contains `backend` and `database` and `timestamp`
    - `apps/web/components.json` exists (shadcn initialized)
    - `apps/web/components/ui/badge.tsx` exists (shadcn badge installed)
    - `apps/web/components/ui/card.tsx` exists (shadcn card installed)
    - `pnpm build` in apps/web exits 0
  </acceptance_criteria>
  <done>
    Health status page renders correctly with three service cards, status badges, summary message, and footer. Next.js build succeeds. The /api/health route checks backend connectivity and returns structured JSON.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>none</files>
  <name>Task 3: Verify full stack health page</name>
  <action>Human verification checkpoint. No automated action -- user visually inspects the running application.</action>
  <what-built>Complete Turborepo monorepo with Next.js health status page, FastAPI backend, and Supabase schema. Running `turbo dev` starts both services; the health page shows live status of all three services.</what-built>
  <how-to-verify>
    1. Start Supabase: `supabase start` (requires Docker running)
    2. Start dev servers: `turbo dev` (starts Next.js on :3000 and FastAPI on :8000)
    3. Visit http://localhost:3000 in browser
    4. Expected: See "Zeee Pitch Zooo" title, "System Status" subtitle, three cards (Frontend, Backend API, Supabase) with green "Healthy" badges, "All systems operational" message, "Phase 1 -- Foundation" footer
    5. Stop FastAPI (Ctrl+C the api process), refresh page
    6. Expected: Frontend still green, Backend API shows red "Unreachable" badge, summary changes to degraded message
    7. Visit http://localhost:8000/api/v1/health directly
    8. Expected: JSON response with `{"status": "ok", "service": "zeee-pitch-zooo-api", ...}`
    9. Run `supabase db reset` to verify migration applies cleanly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <verify>Manual verification -- user confirms visual and functional correctness</verify>
  <done>User approves the health page visual appearance and functional behavior</done>
</task>

</tasks>

<verification>
- `pnpm install` succeeds at root
- `cd apps/api && uv sync` succeeds
- `cd apps/api && uv run pytest tests/test_health.py -x` passes
- `cd apps/web && pnpm build` succeeds
- `supabase db reset` applies migration without errors
- `turbo dev` starts both services concurrently
- Health page at localhost:3000 shows three service cards with live status
</verification>

<success_criteria>
- Turborepo monorepo with pnpm workspaces and uv Python backend fully scaffolded
- Next.js frontend builds and serves health status page
- FastAPI backend runs with /api/v1/health endpoint returning JSON
- Supabase schema has users, documents, chunks tables with pgvector extension and HNSW index
- RLS enabled on all tables with owner-based policies
- Health page displays live service status with three cards and colored badges
- Frontend proxies /api/v1/* to backend via Next.js rewrites
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-demo-content/01-01-SUMMARY.md`
</output>
